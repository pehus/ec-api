framework:
    http_client:
        scoped_clients:
            openstreetmap.client:
                base_uri: '%env(OPENSTREETMAP_BASE_URI)%'
                headers:
                    User-Agent: 'ec-api/1.0'
